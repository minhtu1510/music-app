extends ../../layouts/default.pug
block main
  .forgot-password
        h1(class="text-center") Lấy lại mật khẩu
        form(
          action="/auth/password/forgot"
          method="POST"
        )
          div(class="form-group")
            label(for="email") Email
            input(
              type="email"
              class="form-control"
              id="email"
              name="email"
              required
            )
          div(class="form-group")
            button(
              type="submit"
              class="btn btn-primary btn-block"
            ) Gửi mã OTP
        a(href="/auth/login") Bạn đã nhớ mật khẩu? Đăng nhập ngay