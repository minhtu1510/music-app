.account 
    .boostAccount 
        -if(!users || users.type_user=="basic")
            button(onclick=`handleCheckPremium(event,${JSON.stringify(users)})`) Nâng cấp tài khoản 
    -if (users)
        .avatarAccount 
            img(src="images/img-avatar.jpg" alt="" user=users)